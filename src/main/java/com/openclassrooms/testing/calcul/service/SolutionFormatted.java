package com.openclassrooms.testing.calcul.service;

public interface SolutionFormatted {
	/**
	 * Effectuer le formatage d'un entier en chaîne de caracteres
	 * 
	 * @param le nombre à formater
	 * @return le nombre formaté
	 */
	String format(Integer response);

}
